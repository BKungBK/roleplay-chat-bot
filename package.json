{
  "name": "roleplay-chat-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently --names \"SERVER,WEB\" -c \"bgMagenta.bold,bgCyan.bold\" \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "dev:server": "cd server && bun run src/index.ts",
    "dev:web": "cd web && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
